name: Habliko Mastodon Publisher

on:
  schedule:
    # Dos veces al dia (UTC), desfasado de Blogger y WordPress.
    - cron: "0 11 * * *"
    - cron: "0 21 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: habliko-mastodon-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Publish
        env:
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          MASTODON_TOKEN: ${{ secrets.MASTODON_TOKEN }}
        run: python3 publish.py

      - name: Commit progress
        if: success()
        run: |
          git config user.name "habliko-bot"
          git config user.email "bot@habliko.com"
          if [ -n "$(git status --porcelain progress.json)" ]; then
            git add progress.json
            git commit -m "chore: avance de progreso [skip ci]"
            git push
          else
            echo "Sin cambios en progress.json"
          fi
